<UserControl x:Class="TradingJournal.wpf.Views.JournalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E2E">

    <UserControl.Resources>

        <!-- Stil för formuläretiketter -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <!-- Stil för textrutor -->
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <!-- Stil för primärknapp -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#89B4FA"/>
            <Setter Property="Foreground" Value="#1E1E2E"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>

        <!-- Stil för sekundärknapp -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#45475A"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ── Rubrik ── -->
        <TextBlock Grid.Row="0"
                   Text="Trading Journal"
                   Foreground="#CDD6F4"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- ── Formulär för ny trade ── -->
        <Border Grid.Row="1"
                Background="#181825"
                CornerRadius="8"
                Padding="20"
                Margin="0,0,0,20">
            <StackPanel>

                <TextBlock Text="Lägg till trade"
                           Foreground="#89B4FA"
                           FontSize="14"
                           FontWeight="Bold"
                           Margin="0,0,0,16"/>

                <!-- Rad 1: Asset, Datum, Long/Short -->
                <Grid Margin="0,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Asset" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Asset, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource InputStyle}"
                                 CharacterCasing="Upper"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Datum" Style="{StaticResource LabelStyle}"/>
                        <DatePicker SelectedDate="{Binding CloseDate}"
                                    Background="#313244"
                                    Foreground="#CDD6F4"
                                    Margin="0,0,0,12"/>
                    </StackPanel>

                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Riktning" Style="{StaticResource LabelStyle}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <RadioButton Content="Long"
                                         IsChecked="{Binding IsLong}"
                                         Foreground="#A6E3A1"
                                         Margin="0,0,16,0"/>
                            <RadioButton Content="Short"
                                         IsChecked="{Binding IsLong, Converter={StaticResource InverseBoolConverter}}"
                                         Foreground="#F38BA8"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>

                <!-- Rad 2: Entry, SL, TP, Exit -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Entry" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Entry, UpdateSourceTrigger=LostFocus}"
                                 Style="{StaticResource InputStyle}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Stop Loss" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding StopLoss, UpdateSourceTrigger=LostFocus}"
                                 Style="{StaticResource InputStyle}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Take Profit" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding TakeProfit, UpdateSourceTrigger=LostFocus}"
                                 Style="{StaticResource InputStyle}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="6">
                        <TextBlock Text="Exit" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding ExitPrice, UpdateSourceTrigger=LostFocus}"
                                 Style="{StaticResource InputStyle}"/>
                    </StackPanel>
                </Grid>

                <!-- Rad 3: Amount, Deposit, Withdraw -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Amount (enheter)" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Amount, UpdateSourceTrigger=LostFocus}"
                                 Style="{StaticResource InputStyle}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Insättning ($)" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Deposit, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource InputStyle}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Uttag ($)" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Withdraw, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource InputStyle}"/>
                    </StackPanel>
                </Grid>

                <!-- Anteckningar -->
                <TextBlock Text="Anteckningar" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource InputStyle}"
                         Height="60"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"/>

                <!-- Felmeddelande -->
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="#F38BA8"
                           FontSize="12"
                           Margin="0,0,0,8"
                           Visibility="{Binding ErrorMessage,
                               Converter={StaticResource StringToVisibilityConverter}}"/>

                <!-- Knappar -->
                <StackPanel Orientation="Horizontal">
                    <Button Content="➕  Lägg till trade"
                            Command="{Binding AddTradeCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"/>
                    <Button Content="🗑  Rensa"
                            Command="{Binding ClearFormCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"/>
                </StackPanel>

            </StackPanel>
        </Border>

        <!-- ── Journaltabell ── -->
        <Border Grid.Row="2"
                Background="#181825"
                CornerRadius="8"
                Padding="20">
            <DataGrid ItemsSource="{Binding Trades}"
                      SelectedItem="{Binding SelectedTrade}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Background="#181825"
                      Foreground="#CDD6F4"
                      BorderThickness="0"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#313244"
                      RowBackground="#181825"
                      AlternatingRowBackground="#1E1E2E"
                      ColumnHeaderHeight="36"
                      RowHeight="36"
                      SelectionMode="Single"
                      CanUserResizeRows="False">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#313244"/>
                        <Setter Property="Foreground" Value="#89B4FA"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="12,0"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="12,0"/>
                        <Setter Property="Foreground" Value="#CDD6F4"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#313244"/>
                                <Setter Property="Foreground" Value="#CDD6F4"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Datum"
                                        Binding="{Binding CloseDate, StringFormat=yyyy-MM-dd}"
                                        Width="100"/>
                    <DataGridTextColumn Header="Asset"
                                        Binding="{Binding Asset}"
                                        Width="80"/>
                    <DataGridTextColumn Header="L/S"
                                        Binding="{Binding TradeType}"
                                        Width="60"/>
                    <DataGridTextColumn Header="Entry"
                                        Binding="{Binding Entry, StringFormat=N2}"
                                        Width="90"/>
                    <DataGridTextColumn Header="SL"
                                        Binding="{Binding StopLoss, StringFormat=N2}"
                                        Width="90"/>
                    <DataGridTextColumn Header="TP"
                                        Binding="{Binding TakeProfit, StringFormat=N2}"
                                        Width="90"/>
                    <DataGridTextColumn Header="Exit"
                                        Binding="{Binding ExitPrice, StringFormat=N2}"
                                        Width="90"/>
                    <DataGridTextColumn Header="Amount"
                                        Binding="{Binding Amount, StringFormat=N8}"
                                        Width="110"/>

                    <!-- PnL med färgkodning via DataGridTemplateColumn -->
                    <DataGridTemplateColumn Header="PnL ($)" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding PnL, StringFormat=N2}"
                                           Padding="12,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#A6E3A1"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding PnL,
                                                    Converter={StaticResource IsNegativeConverter}}"
                                                    Value="True">
                                                    <Setter Property="Foreground" Value="#F38BA8"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Anteckningar"
                                        Binding="{Binding Notes}"
                                        Width="*"/>
                </DataGrid.Columns>

                <!-- Högerklicksmeny för att ta bort trade -->
                <DataGrid.ContextMenu>
                    <ContextMenu Background="#313244">
                        <MenuItem Header="🗑  Ta bort trade"
                                  Command="{Binding DeleteTradeCommand}"
                                  Foreground="#F38BA8"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>

            </DataGrid>
        </Border>
    </Grid>
</UserControl>